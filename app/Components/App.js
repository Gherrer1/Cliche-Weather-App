const React = require('react');
const Router = require('react-router-dom').BrowserRouter;

function App() {
	return (
		<Router>
			<div>
				<nav>Fake Nav</nav>
			</div>
		</Router>
	);
}

module.exports = App;
